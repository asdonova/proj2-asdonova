#include "Heap.h"
#include <iostream>
#include <string>
#include <fstream>
void swap(int *x, int *y);

bool isInitialized(Heap* heap);

bool isEmpty(Heap* heap);

bool getFileSize(int size);

void printMenu();

